{% macro content(spec) %}

{% set spec = spec or {} %}
{% set _text = spec.text or 'Default Section Title' %}
{% set _bar_color = spec._bar_color or 'primary' %}


<h2 class="section-title section-title--{{_bar_color}}">
  {{ _text }}
</h2>
{% endmacro %}
