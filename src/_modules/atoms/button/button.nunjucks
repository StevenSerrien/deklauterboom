{% macro content(spec) %}

{% set spec = spec or {} %}
{% set _text = spec.text or '' %}
{% set _class = spec.class or '' %}

<div class="{{_class}}">
  <p>This is an example paragraph for the button module</p>
</div>
{% endmacro %}
