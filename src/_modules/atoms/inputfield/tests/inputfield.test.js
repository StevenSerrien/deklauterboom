'use strict';

import Inputfield from '../inputfield';

describe('Inputfield View', function() {

  beforeEach(() => {
    this.inputfield = new Inputfield();
  });

  it('Should run a few assertions', () => {
    expect(this.inputfield);
  });

});
