'use strict';

import InfoBlock from '../info-block';

describe('InfoBlock View', function() {

  beforeEach(() => {
    this.infoBlock = new InfoBlock();
  });

  it('Should run a few assertions', () => {
    expect(this.infoBlock);
  });

});
